# r1_c1_transient_control_lowering: pre-experiment boundary

Baseline: b7882b00f2f439812af1dfc4f8990f83a83f03e7 (resolved full Git ID;
the assignment abbreviated it by its final character). Source equivalent to
cee3c256c1639546c0a35d02a7b19690d45b4809 except campaign-state documentation.
Assigned worktree: /tmp/chr-cycle7-opt-transient-control-lowering.

Hypothesis: a source-certified finite deterministic chain with exactly one
consuming single-head rule per control signature can represent each original
control occurrence with a resumable continuation. Avoiding graph insertion,
activation, general commit consumption and associated collection/release should
reduce representation/work costs on fresh-contract 64 and 256, rows=4, depth=4.
Every copy still allocates fresh variables at every original application and
publishes every application boundary. Residual cell/mark/end facts remain real.
No matching optimization, fresh-value memoization or chain inlining is proposed.

Potential certificate: inspect Prepared rules/triggers and body instructions,
not relation names. Exactly one consuming non-repeating head; deterministic
finite body; at most one next control; acyclic control dependency graph. Reject
unsupported consumers, propagation, conditional choices and alias/escape cases.
Whole-program and query checks must cover arrivals outside the selected body.
Runtime observation escape requires a proven fallback, not merely history=false.

Introduced costs to charge: preparation/certificate storage; occurrence-local
continuation identity and arguments; fresh variables; FIFO scheduler admission;
pending completion/syntax obligations; capture/fallback; cancellation and final
release. A reduction confined to graph counters is not sufficient evidence.

Primary diagnostic cases: fresh-contract 64 and 256, rows=4, depth=4, grouped
(the supplied raw configuration is grouped). Secondary: fresh-unmerged with
the same dimensions, rewrite128, and late observation/retention of this same
control shape. Existing independent fresh oracle and source application counts
are mandatory. Total applications are 2,048/8,192, of which 768/3,072 are
normalization applications. Unmerged totals are 1,280/5,120.

Falsifiers: failure to preserve occurrence/freshness multiplicity, source
boundaries, fairness or observation; displaced allocation/retention negating
avoided work; or failure to reclaim all execution objects. Unsupported scope
is BLOCKED/unassessed, not evidence that the architectural idea is slower.

Budget: use the assigned cycle's one-hour bound; first establish whether a
compatible observation fallback is actually bounded. Do not consume timing
ownership or perform descriptive timing comparisons without a grant. No timing
acceptance gate, alpha, runtime effect bound or runtime ranking applies. If no
valid executable candidate exists, do not manufacture a baseline/candidate A/B
comparison by relabeling identical binaries. Record missing gates explicitly.

Initial source finding: capture_snapshot/start_inspection(None)/request_step are
available after arbitrary ordinary progress. Snapshots own an immutable graph
and pending-syntax root. Removing a committed control and leaving only pending
Post syntax changes the snapshot contract; immediately applying the consumer
removes an existing source-observable boundary. Confirm with a focused symmetric
semantic fixture before deciding whether implementation is honestly bounded.
