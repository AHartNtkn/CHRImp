# Current optimization cycle

- Status: cycle 5 complete; demand-built matching arrangements integrated as `995f4a6` + `5814fa6` and validated. Earlier compiler, archive ownership and selective inspection sharing remain integrated.
- Automation: `chr-architecture-optimization`, hourly heartbeat in this thread.
- Repository: `/home/ahart/Documents/CHRImp`; landing branch `codex/shared-relational-engine`.
- Production: structural normalization, conditional dispatch, source-derived consumer exclusions before constructor choices, incremental archive ownership, demand-built matching arrangements, and shared inspection results retained at reader endpoints and active computation frontiers. CLI and notebook share the executor; history and source stepping remain optional.
- Latest decision status: compiler and ownership selected and integrated. Combined four-answer identity uses 325 choices, 1,761 posts and 156.81 MB requested allocation versus baseline 492, 2,487 and 257.87 MB. Largest reader case allocates 120.46 MB versus 189.22 MB with the same retained conditions. All ten combined diagnostic observations completed, including unchanged semantic-work controls. Evidence: `/tmp/chr-reassessment/integration.json`; allocation units follow [reassessment](optimization-reassessment.md).
- Clean committed experiments: `/tmp/chr-loop3-producer-consumer`, `codex/spike-producer-consumer`, `f5c8648`; `/tmp/chr-loop3-demand-support`, `codex/spike-demand-support`, `f108957`. The latter has explicitly incomplete validation; do not treat completing every experimental path as outstanding delivery.
- Cycle 3 used about 35.48 seconds measured execution of 120 allowed: 78 observations and one CPU profile. Raw evidence under `/tmp/chr-loop3/` and `/tmp/chr-loop3-support-profile/`. Its budget/experiment is closed; do not repeat without a changed premise.
- Next action: continue the authorized optimization loop from the integrated engine. Use maintained diagnostics to establish the next material problem before brainstorming; previous bottleneck measurements may no longer apply. Reverse ordering remains shape-dependent, overlap a construction/traversal tradeoff, relative completion without general demonstrated gain, and the expression backend adverse on exact queries. None independently requires further development. Never use program runtimes to determine gains.
- Earlier experiment records: [cycle 2](optimization-cycle-2.md), projected overlap `950d747` and relative completion `ec1822f`; [cycle 1](optimization-cycle-1.md), ownership `861dc54` and reverse order `737090f`. Their runtime-based rankings must not control subsequent work.
- Earlier architectural decision: `docs/goals/architecture-exploration/notes/decision.md`. Static whole-head feasibility remains separate and unintegrated. Hard behavior synthesis remains unresolved; use “timed out without an answer” in user-facing results.

## Cycle 4: bounded variant assessment complete

Started 2026-09-13 17:59 UTC from `2f2f8f8`. Original budget: one hour active investigation, 120 seconds serialized measurement. Measurements used approximately 22 seconds including the fresh profile and structural follow-ups. Existing baseline evidence was reused. No program runtime determines a gain or integration decision.

Diagnosis: four rotating readers at depths32/64/128 retain701/2,413/9,039 conditions while semantic tasks grow276/500/948. Chronological Birth supports retain increasingly long survivor prefixes; ascending BDD order reconstructs those prefixes. Maintained profile `/tmp/chr-loop4-profile/flame.svg` locates condition collection and Boolean execution as major consumers. These are representation and repeated-work questions, not excess derivations.

### Integrated: selective sharing of inspection restrictions

Production commit `7fae280`, experiment `e7d8d6a` in `/tmp/chr-loop4-selective`. Prefix metadata shares exact selection paths; expanded results belong to reader endpoints and active computation frontiers. Overlapping readers require8 shared jobs versus10 baseline; a later identical request reuses a completed result. Independent selections retain linear state for16/32/64 choices with one or two readers, and all restriction metadata is reclaimed after release. New-demand reconstruction and metadata lookup remain costs.

Reader128 all-process allocation120.812→104.437MB; inspection allocation67.949→51.499MB. Sixteen maintained observations complete with unchanged ordinary-control semantic work. Full Rust targets with and without diagnostics, doc tests, and Python observation-mode checks pass. Evidence: `/tmp/chr-reassessment/cycle4-selective.json`, `variant-controls-selective.json`, and the production independent-selection retention regression. This meaningful retention variant establishes inspection reuse without the initial implementation's quadratic retained results.

### Unintegrated: indexed prefix representation

Experiment `c62b81b` in `/tmp/chr-loop4-indexed` combines prefix-first placement with a balanced observation index, borrowed necessary envelopes and nonmaterializing overlap checks. It addresses the initial chronological-scanning implementation's contingent failure: wide24/48/64 birth visits are23/47/63, index visits104/236/316, and allocation0.824/2.151/2.658→0.596/1.527/1.882MB. Reader128 held conditions9,039→264. These are real mechanism improvements.

However, completed identity2 uses309.330MB versus105.242MB baseline, with compaction Boolean actions30,757→3,319,747. Births229→238 and posts1,259→1,327 are not identical execution work, but cannot by themselves establish the cause of this much larger compaction cost. An alias/backpressure check also fails to finish within its existing dispatch cap; an extended diagnostic produces32 answers with2,006,079 observer dispatches. Its cap is not altered and elapsed runtime is not a verdict.

A second, distinct structural combination in `/tmp/chr-loop4-indexed-overlap` tests existing projected-overlap queries against that compaction cost. At the identical two-answer endpoint and identical births/posts to indexed alone, it reduces transform actions166,101→105,753, but compaction Boolean actions remain3,319,747 and total allocation307.674MB. Thus it avoids some construction without addressing the dominant measured work. Six predicate tests,11 diagnostic tests, formatting and release measurement build pass; the14-case maintained cohort was also exercised. It is not an integration candidate.

Evidence: `/tmp/chr-reassessment/cycle4-indexed.json`, `variant-controls-indexed.json`, `variant-identity2-indexed.json`, `cycle4-indexed-overlap.json`, `variant-identity2-indexed-overlap.json`; unchanged baseline `integration.json` and `cycle4-baseline.json`. Original experiments are preserved as `cc6dbe1` (prefix placement with chronological scanning) and `5ef0ad8` (inspection sharing), with retention probe `5853e73`.

Decision: integrate selective retention; do not integrate either indexed configuration. The indexed variant resolves the original scanning regression, so that failure does not refute prefix sharing. The new compaction and observation costs remain unresolved rather than established as essential to the idea. This bounded assessment is complete; further variants are not independently owed. If subsequent diagnosis returns to this idea, first identify which remaining compaction operations create the large Boolean work and whether their representation can be avoided; do not continue tuning projected overlap merely because it has improvement opportunities. Continue the authorized loop from current production evidence and choose the next material architectural question on that basis.

## Cycle 5: demand-built matching arrangements

Started 2026-09-13 19:02 UTC from `4069614` (the validated implementation simplification pass). Phase: complete. Budget: one hour active investigation/implementation including meaningful variants,120 seconds serialized measurement. Rotate to multihead correlation and join planning: measure existing partial-join miss/hit32/128/512 at8probes, rejected triangle16/32/64, and unchanged rewrite128. Native3s/50million steps, external8s/1GiB per observation, initial30s aggregate. No solution is selected before work/allocation attribution and source diagnosis. Evidence will be under `/tmp/chr-cycle5/`.

Cycle5 diagnosis established before brainstorming: maintained14 diagnostic observations complete (`/tmp/chr-cycle5/baseline.json`, `baseline-probes.json`), using0.45measurement seconds. Existing `partial-join` program is `probe(X,Y,I),row(X,Y,Z) ==> hit(I,Z)` with distractors `row(A,Ci,Li)` and `row(Di,B,Ri)` and repeated probes `probe(A,B,Ij)`. No matches. At128distractor pairs,8/32/128probes visit1,288/4,384/16,768candidates; at512pairs visits5,128/17,440/66,688 and matching dispatches102,832/337,600/1,276,672. Total allocation grows27.33→35.19→66.78MB for the512sweep. Candidate visits equal n*(probes+2)+probes: the same n incompatible rows are inspected per probe, beyond the linear anchor visits. Successful-join controls preserve8applications; rejected-triangle visits scale linearly, and rewrite128 is the unchanged control.

Source mechanism: `Matches::new` starts an independent matcher per anchor; `Phase::Select` in `src/matching.rs` chooses a single bound-port bucket. Whole-tuple lookup is enabled only when every argument is bound and singleton. With two known ports and unbound payload Z, it scans one bucket then tests the other bound port, repeating that work for each probe. Graph port indexes and occurrence storage are shared; the correlated restriction is not. Each scanner pins its immutable root, and equality/occurrence/liveness checks remain required. Fresh maintained profile `/tmp/chr-cycle5-profile/flame.svg` (585samples, completed partial-join4096/probes128) supports attribution to matching/store/identity rather than output cardinality. Question for architectural experiments: eliminate repeated incompatible candidate enumeration without transferring larger costs to updates, aliases, retained snapshots or productive joins; determine what sharing/representation/algorithm can do this while preserving full semantics. Counts are evidence, not mandated internal mechanisms.

Cycle5 selection (19:08UTC): five independent brainstormers with compiler, relational-algorithm, incremental-sharing, fairness/ownership and adversarial-cost priorities recommend related but distinct mechanisms. Primary references: [automatic index selection](https://www.vldb.org/pvldb/vol12/p141-subotic.pdf), [Leapfrog Triejoin](https://arxiv.org/abs/1210.0481), [shared arrangements](https://arxiv.org/abs/1812.02639), [incremental LFTJ](https://arxiv.org/abs/1303.5313), [factorized IVM](https://arxiv.org/abs/1703.07484). Static join results do not establish CHR conditional-identity or update bounds. Whole-graph-root memoization is insufficient because unrelated probe postings change that root; blindly intersecting posting lists can retain the same linear scan per probe. Universal eager partial-match materialization has an adverse productive Cartesian state cost and is not selected.

Two initial20-minute structural spikes from4069614, reserving the remaining cycle budget for variants/validation: `/tmp/chr-cycle5-projections` (`codex/spike-projection-indexes`) tests compiler-derived correlated-port indexes shared across readers, expecting incompatible candidate work to become input-linear plus lookups. `/tmp/chr-cycle5-restrictions` (`codex/spike-shared-restrictions`) tests shared demand restrictions with dependency-valid reuse across distinct anchors/graph roots, expecting one evaluation per relevant version rather than per probe. Both charge preparation, index/cache maintenance, supported equality, productive enumeration, pinned versions and release. Current measurement budget used~1.72seconds including585-sample CPU profile. Initial maintained comparisons reuse14baseline observations; adverse controls include productive partial joins, aliases, multiplicity, triangle/rewrite and continued readers. A distinct-key probe discriminates indexing from repeated-key caching; late enabling identity and stale snapshots discriminate dependency validity. Negative/mixed implementations must receive the bounded fair-trial assessment; no microoptimization or blanket rejection of their idea.

Cycle5 comparisons: baseline and both initial 31-case cohorts completed with equivalent application/post/birth counts and exact distinct-key/productive residual oracles. Evidence `/tmp/chr-cycle5/{baseline,projections,restrictions,arrangements}{,-probes,-controls,-distinct,-patterns,-productive}.json`. Eager `a8d322a` eliminates incompatible candidate enumeration even for distinct keys, but maintains undemanded patterns:16-port control21.707MB versus19.885MB baseline. Exact-key `b594058` shares repeated identical restrictions but distinct32 still scans32,768producer rows plus3,072consumer candidates. That is relocation for distinct keys, not elimination.

The bounded fair-trial variant `c96d94b` shares a demand-built arrangement per selected port bucket, projection mask and exact subtree version. Subscriber-specific values select independent append-only partition streams. At distinct8/16/32, producer reads64/272/1,056 replace512/4,096/32,768; consumer visits192/768/3,072 remain separately charged. Retained rows peak64/256/1,024 and return to zero. At distinct32 total allocation94.334→74.115MB; at512rows/128repeated probes66.783→27.923MB, with total row inspections66,688→1,664. Unqueried-pattern16 allocation19.885MB remains essentially unchanged. Reader64 and synthesis identity controls preserve semantic work and allocation within small observation/configuration overhead; no synthesis architectural gain is claimed.

Adverse update test: local `/tmp/chr-cycle5-update-probe.py` and `{baseline,projections,arrangements}-updates.jsonl` hold an earlier snapshot while issuing eight distinct misses against unchanged or appended buckets. At512rows with appends, arrangement still inspects4,132producer rows plus8consumer candidates versus baseline4,140candidates: version rebuilding remains. Probe allocation2.486→1.755MB, update allocation21,056→22,592bytes, retained partition rows4,132 across versions. With unchanged bucket, probe allocation2.465→0.222MB and inspections4,104→520. Current/held-snapshot sentinel oracles and cleanup pass at32/128/512. Eager indexing handles sparse changing demand better, but combining two representations would duplicate maintenance; defer that policy question until workload evidence warrants incremental maintenance of demanded arrangements.

Decision: integrate demand arrangements (`995f4a6` + `5814fa6`), not eager projections or a second exact-key representation. This changes the sharing granularity and removes repeated distinct-key scans on stable buckets, without material adverse allocation in the tested controls. Applicability remains singleton-certified bound identities, up to8ports and4,096rows per bucket, with32cached entries; other matches retain full general semantics. Bucket changes and collection can reconstruct arrangements. These are explicit configuration/ownership limits, not claims that the indexing idea is exhausted. No constant tuning or further variants are required for this scoped decision.

Maintained fresh profile `/tmp/chr-cycle5-arrangement-profile/flame.svg` completed on the same4096row/128probe source. Its stack distribution moves from matching/identity toward graph updates, collection and release, consistent with the structural counts; sample totals or execution runtimes do not determine the integration decision. Total serialized measurement remains under8seconds of120, including two profiles and update checks. Source diagnosis, five-priority brainstorming, initial implementations, fair variant and comparisons all belong to the original cycle budget. Validation passed:444 ordinary and470 diagnostics Rust tests;3doc tests in each mode; focused final matching/CLI checks,13maintained perf parser tests, formatting and Clippy with diagnostics/all targets. CLI diagnostic checkpoints expose producer work and retention alongside matcher work. Logs `/tmp/chr-cycle5-restrictions-validation/` and `/tmp/chr-cycle5/{cli-diagnostics-test,final-matching-tests,perf-parser-tests,clippy}.log`. Cycle5 is complete. Next wake: diagnose the integrated system before selecting another material architectural problem; do not treat further arrangement tuning or incremental maintenance as owed work.
