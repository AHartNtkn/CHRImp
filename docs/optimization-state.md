# Current optimization cycle

- Status: source-derived producer/consumer compilation and incremental archive ownership integrated on the landing branch. Combined diagnostic checks preserve both demonstrated gains.
- Automation: `chr-architecture-optimization`, hourly heartbeat in this thread.
- Repository: `/home/ahart/Documents/CHRImp`; landing branch `codex/shared-relational-engine`.
- Production: structural normalization, conditional dispatch, source-derived consumer exclusions before constructor choices, and incremental archive ownership. CLI and notebook share the executor; history and source stepping remain optional.
- Latest decision status: compiler and ownership selected and integrated. Combined four-answer identity uses 325 choices, 1,761 posts and 156.81 MB requested allocation versus baseline 492, 2,487 and 257.87 MB. Largest reader case allocates 120.46 MB versus 189.22 MB with the same retained conditions. All ten combined diagnostic observations completed, including unchanged semantic-work controls. Evidence: `/tmp/chr-reassessment/integration.json`; allocation units follow [reassessment](optimization-reassessment.md).
- Clean committed experiments: `/tmp/chr-loop3-producer-consumer`, `codex/spike-producer-consumer`, `f5c8648`; `/tmp/chr-loop3-demand-support`, `codex/spike-demand-support`, `f108957`. The latter has explicitly incomplete validation; do not treat completing every experimental path as outstanding delivery.
- Cycle 3 used about 35.48 seconds measured execution of 120 allowed: 78 observations and one CPU profile. Raw evidence under `/tmp/chr-loop3/` and `/tmp/chr-loop3-support-profile/`. Its budget/experiment is closed; do not repeat without a changed premise.
- Next action: continue the authorized optimization loop from the integrated engine. Use maintained diagnostics to establish the next material problem before brainstorming; previous bottleneck measurements may no longer apply. Reverse ordering remains shape-dependent, overlap a construction/traversal tradeoff, relative completion without general demonstrated gain, and the expression backend adverse on exact queries. None independently requires further development. Never use program runtimes to determine gains.
- Earlier experiment records: [cycle 2](optimization-cycle-2.md), projected overlap `950d747` and relative completion `ec1822f`; [cycle 1](optimization-cycle-1.md), ownership `861dc54` and reverse order `737090f`. Their runtime-based rankings must not control subsequent work.
- Earlier architectural decision: `docs/goals/architecture-exploration/notes/decision.md`. Static whole-head feasibility remains separate and unintegrated. Hard behavior synthesis remains unresolved; use “timed out without an answer” in user-facing results.

## Active cycle 4

Started 2026-09-13 17:59 UTC from `2f2f8f8`. Phase: initial experiments complete; bounded fair-trial assessment of meaningful variants remains open. Neither tested configuration is selected for integration. Budget: one hour active investigation, 120 seconds serialized measurements; Approximately 9.1 seconds used including the fresh profile, 34 supervised observations and the targeted retention probe. Follow-ups share the original cycle budget; no fresh allowance is granted. Integration observations from the preceding cycle are reused, not rerun.

Diagnosis: four rotating readers at depths 32/64/128 retain 701/2,413/9,039 condition nodes and allocate 10.58/32.90/120.46 MB in the integrated engine (`/tmp/chr-reassessment/integration.json`). Reader count and payload width are fixed; semantic tasks grow 276/500/948. The supports retained by chronological `Birth` records form increasingly long survivor prefixes for `turn() <=> turn(),(fail;true).` (`examples/measure/interactions.rs`, `src/engine.rs`). Ascending BDD order reconstructs earlier prefix nodes when appending later decisions; the `fresh_scoped_choice` predecessor bound prevents sifting across them (`src/condition.rs`). Existing prefix witnesses and opposite-order experiment confirm a representation effect, not excess derivations. Archive ownership shares reachability bookkeeping, but cannot shrink these retained functions.

Fresh maintained profile `/tmp/chr-loop4-profile/flame.svg` completes at depth 256: 1,168 CPU samples, including 577 in condition collection and 317 in Boolean Job execution (inclusive, overlapping). Inspection enters birth/row support indexing and Boolean intersections (`src/observe.rs`, `src/observe/index.rs`). These samples locate consumers of the growing representation; they are not gains or exclusive costs. Exact-query cofactor growth and wide alternatives are known adverse controls for previous representations. Uncertainty for experiments: can a changed representation/algorithm avoid prefix reconstruction or repeated query work while preserving unrelated-choice and inspection behavior, without transferring the cost to classification, release, or another graph shape? No incumbent rank/counter is a semantic constraint.

Tested decisions:
- `/tmp/chr-loop4-prefix`, `codex/spike-prefix-observation`, `cc6dbe1`: prefix placement plus chronological observation cuts reader128 held conditions 9,039→264 and scoped allocation120.46→76.85MB. Wide24/48/64 allocation instead grows0.824/2.151/2.658→0.970/2.958/4.665MB at matched answers/births. New birth visits289/1,167/2,026 show quadratic repeated scanning across histories. This is real prefix sharing with an adverse observation tradeoff, not an unconditional winner. Some larger semantic-delivery cases remain unvalidated at their fixed caps; those caps are not the gain verdict.
- `/tmp/chr-loop4-inspection`, `codex/spike-shared-restriction`, `5ef0ad8` plus probe `5853e73`: live selection-prefix reuse reduces reader128 scoped allocation120.46→104.02MB (inspection67.95→51.50MB); including admission/reporting allocation120.81→104.43MB. Ordinary controls preserve semantic work and essentially unchanged allocation. Direct proof:8 jobs with overlap versus baseline10; isolated new path12. However, selecting16/32/64 independent choices while one or two readers hold output retains136/528/2,080 condition nodes versus baseline31/63/127, after physical GC. Both release to zero. Caching all expanded prefixes introduces quadratic live retention, so no integration. Selective retention remains an untested architectural variant; this result does not refute inspection reuse.

Evidence: `/tmp/chr-reassessment/cycle4-prefix.json`, `cycle4-inspection.json`, `cycle4-baseline.json`, `cycle4-readers.json`; `/tmp/chr-loop4-restriction-retention/{baseline,spike}.txt` contains the identical retention probe. Main adds a focused independent-selection retention regression test, passing with and without diagnostics. Production remains `2f2f8f8` engine code.

Next action: apply the goal’s bounded fair-trial rule to the contingent failures. Evaluate indexed observation with prefix sharing and selective retention of shared restriction results; choose the highest-value affordable variant from the existing diagnosis. Neither exhaustive variant development nor microoptimization is required. Keep the measured wide-choice and independent-selection cases as adverse controls. Reuse the current evidence and original budget, then report a configuration decision or explicitly unresolved idea. Relative scopes and structured decomposition remain alternative hypotheses, not required deliverables.
