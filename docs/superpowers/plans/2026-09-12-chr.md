# CHR Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development for independent components and review each integrated task.

**Goal:** Implement the complete approved relational language and local notebook.

**Architecture:** One shared graph records occurrence liveness, variable identity, and work under conditions on explicit choices. Indexed, resumable discovery and serialized conditional updates share applications across alternatives. Causal choice births preserve answer multiplicity independently of condition simplification.

**Tech Stack:** Rust execution process; serde for a common JSON program/inspection model; browser HTML, CSS, JavaScript and SVG for the notebook.

**Spec:** ../../../ARCHITECTURE.md

## Global constraints

- Relation arguments contain only variables; ordinary relations have no built-in term theory.
- Matching does not merge variables. Explicit body equality does. Body locals are fresh per application.
- Only explicit disjunction creates alternatives; other competing applications commit.
- Propagation is once per rule and ordered occurrence tuple per alternative.
- Answers are residual normal forms, with no deduplication.
- Fairness includes internal work, observation and completion; history is opt-in and off by default.
- Both text and direct diagram editing, all specified graph views, step/run/pause/resume and alternative inspection are deliverables.
- Validate general relational regimes, shared execution, low-sharing overhead and continuing reclamation.

## Task 1: Language and condition foundations

Files: `src/syntax.rs`, `src/condition.rs`, `src/program.rs`, `tests/syntax.rs`, `tests/condition.rs`, `tests/program.rs`, `Cargo.toml`, `src/lib.rs`.

- [x] Define serde program types: `Atom { relation, args }`, `Rule { name, kept, removed, body }`, and recursive `Body` with conjunction, disjunction, relation posting, identification, success and failure. Names are strings and arguments are variable names; parser validates variable-only arguments and nonempty rule heads.
- [x] Write executable parse/format round-trip and rejection tests before implementation. Include simplification, propagation, simpagation, nested choice and fresh locals.
- [x] Implement source parsing/formatting and validation against those tests. Text and diagrams must serialize the identical model.
- [x] Test canonical Boolean conditions against full truth tables, interleaved resumable jobs, zero-work Boolean identities and collection with live suspended jobs.
- [x] Implement hash-consed conditions and iterative apply jobs, bounded caches, explicit live roots and resumable reclamation. Support allocation must not retain all dead choices.
- [x] Prepare interned relation/arity signatures, ordered head activation targets, rule variable slots with fresh-local boundaries, and reusable body instructions. Tests preserve nested choice and duplicate arms.
- [x] Run `cargo test --offline`; review and commit the integrated foundations.

## Task 2: Shared relational state and indexed matching

Files: `src/graph.rs`, `src/matching.rs`, `tests/graph.rs`, `tests/matching.rs`.

- [ ] Test repeated-variable heads, distinct occurrences, conditional merging enabling cross-predicate joins, high-degree identity repair, long chains and arbitrary cycles.
- [ ] Implement stable occurrence/variable identities, conditional representative links, predicate and ordered-port indexes, and resumable supported tuple discovery. Prepare reusable rule access plans.
- [ ] Test persistent current-root inspection, dependency-specific invalidation, partial-support consumption, and collection preserving active cursors.
- [ ] Implement staged updates with a serial mutation owner and coherent root publication; verify and commit.

## Task 3: CHR execution, search and observation

Files: `src/engine.rs`, `src/observe.rs`, `tests/semantics.rs`, `tests/progress.rs`, `tests/lifecycle.rs`.

- [ ] Test all three CHR head modes, fresh locals per application, overlapping propagation support, no implicit rule search, explicit nested-choice multiplicity, failure and normal-form answers.
- [ ] Implement application events, causal choice births, fair resumable obligations and conditional completion certificates. Revalidate all heads before commit.
- [ ] Test finite answers beside divergent work, early failure, common post-choice chains, reconnection after independent work, and cancellation/delivery ownership.
- [ ] Implement resumable projection with bounded pending output, ordinary execution without history, explicit snapshot retention and optional recording.
- [ ] Measure and test continuing collection against stable live state, separately from necessarily growing search frontiers; verify and commit.

## Task 4: Local process and notebook

Files: `src/main.rs`, `src/server.rs`, `web/index.html`, `web/notebook.js`, `web/notebook.css`, `tests/server.rs`, `web/notebook.test.js`.

- [ ] Test actual parse/start/advance/inspect/pause/resume/step/cancel request flows against the engine, including malformed requests and immutable submitted programs.
- [ ] Implement a loopback process serving notebook assets and query controls with bounded engine service between requests.
- [ ] Build text and direct graph editors on the common program model; draw variable junctions, colored relation nodes and numbered ordered ports, including rule sides and nested body choices.
- [ ] Add query cells, alternative selection, intermediate and answer views, logical stepping, continuous execution and separate recording controls.
- [ ] Exercise both editors and every execution control in the real browser; verify semantic round trips and actual graph results; commit.

## Task 5: Complete validation and usable delivery

Files: `tests/regimes.rs`, `examples/*.chr`, `README.md`, `docs/validation.md`.

- [ ] Exercise sparse/dense joins, alias chains/high-degree merges/cycles, common and distinct branch work, early failure, resource conflicts, long-running recursion, output sizes and cold/warm queries.
- [ ] Measure work, time and retained memory separately per regime against equivalent prepared execution controls from CHRLang. Investigate overhead, preserving all semantics.
- [ ] Add runnable relational examples spanning rewriting, closure/proof construction and synthesis; document grammar, launch and notebook use.
- [ ] Run formatting, lint, tests, release builds and real notebook workflow; obtain independent semantic/quality review and repair findings.
- [ ] Audit every requirement in ARCHITECTURE.md against current evidence before marking the goal complete; commit all task-owned work.
