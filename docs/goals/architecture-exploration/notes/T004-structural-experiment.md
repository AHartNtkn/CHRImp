# Structural normalization experiment

The selected experiment derives structural consistency from the program and makes it a direct operation on supported identity attachments. It retains ordinary structural rows for the remaining evaluator rules and output. The narrower claim is elimination of generic discovery/commitment for the recognized subsystem, not elimination of all structural storage or replacement of conditional identity.

Existing graph roots already version conditional identity and occurrences together. Use that foundation for this experiment because it permits direct attribution while preserving existing computed-work sharing; it is not a constraint on the eventual architecture. If integration cost exposes a reason to replace that foundation, record it as architectural evidence rather than assuming the current model must survive.

## Comparison

A: existing execution. B: normalize the recognized rules with generic matching/commitment before other mutations. C: the same normalization priority using direct conditional attachments and field equalities. Source recognition must use exact rule structure and whole-program interference, not names or indices. The other 14 behavior-synthesis rules remain executable. Experimental admission rejects unsupported normalization configurations explicitly; no language restriction is inferred from that boundary.

The direct normalizer owns a staged graph root, resolves posting roots, compares attachment partitions, installs uncovered descriptions, coalesces only overlapping same-tag occurrences, rejects conflicting overlap, and propagates field equalities through supported union events. Publish normalized state and all induced consumer activations together. Preserve surviving occurrence support outside overlaps. Source terminal failure that consumes a structural row must not expose a stale attachment. Long operations must yield; no enumeration of full search assignments. Cycles remain legal.

## Bounded implementation

Worktree /tmp/chr-arch-structural, branch codex/arch-structural, base f67a3f0. One worker owns the package. Initial implementation/validation budget is 90 minutes; no automatic extension. Include only necessary recognition, storage/union integration, normalization, lifecycle tracing and an experimental command. No notebook work, production default switch, instrumentation framework or general optimizer. Historical per-rule stepping through fused operations need not be implemented in this prototype and must not be presented as supported.

Verify renamed/reordered recognition, conditional overlap and opposite union directions, derived equalities enabling real apply_k/no_c behavior, cycles, fresh/occurrence distinctions, finite sibling progress and common-work sharing, cancellation/collection/compaction through suspended normalization, and valid residual completion. Prefer existing semantic oracles and focused source-level finite cases. Keep the initial unrestricted identity-synthesis query as the practical relevance case; no supplied combinator witness.

After implementation, a serialized campaign has at most 120 seconds total measurement time, native three-second observations and five-second external limits where supported. Use baseline/priority/direct modes, exact supported-answer checks, cold setup and total execution/memory cost. Establish corresponding normalization work on finite controlled mutations; add replay machinery only if an actual attribution ambiguity requires it. Five repetitions at selected useful points suffice initially; do not refine marginal differences.

Advance direct lowering only for a substantial gain over the priority control after counting attachments, Boolean operations, wakes and collection. If only priority helps, identify the scheduling gain. If integration cannot realize direct normalization within the bounded prototype, report the missing responsibility and whether a different foundation is warranted. Neither a lower matcher count nor an incomplete implementation proves success or architectural failure.
