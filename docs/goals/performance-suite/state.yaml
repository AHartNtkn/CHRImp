version: 2
goal:
  title: Complete the CHR performance suite
  status: active
  specification: goal.md
  full_outcome_complete: false
  repository: /home/ahart/Documents/CHRImp

capabilities:
  - Source-backed risk model spans matching, conditional graphs, preparation, output and resource lifetimes.
  - Generated size/shape/seed controls use independent semantic, progress and resource oracles.
  - Routine/deep native runs cover points before repetitions, with declared budgets and honest censoring.
  - Measurements carry interval completion; censored runs retain valid resources and completed cleanup.
  - Control-qualified timing, distribution, scaling and allocation comparisons use maintained commands.
  - CPU/heap profiles and scoped shared-service counters localize work, allocation and retained resources.
  - Conditional-reader cases vary ownership and duration, check projection without source progress and release.
  - Standalone CLI diagnostics and all 17 unrestricted saved synthesis queries have native execution paths.

validation:
  package: Current coherent implementation package validated; full goal remains active.
  diagnostics: 11 integration tests and independent conditional-work/coordinate calibration pass.
  observer_equivalence: Graph-pruning/arena and cleanup-probe deltas explain permitted iteration variation.
  allocation: Optimized observable allocator calibration and allocation extraction/default detection pass.
  conditional_readers: Projection boundaries and default/diagnostics harness tests pass.
  synthesis: Behavior synthesis has 45 baseline observations, nine CPU profiles and two heap profiles; four queries finish and five cap at three seconds.
  challenges: Native latency, residency and coupled interventions detected; unchanged control has no signal.

remaining:
  targeted_diagnoses:
    status: open
    gap: Maintained diagnosis of the expensive conditional-reader run.
  observer_calibration:
    owner: Mendel
    status: results_available
    result: 72 samples completed across rewrite, behavior synthesis and runtime sessions.
    limits: Sequential mode order can confound drift; seven observations per campaign limit precision.
    evidence: /tmp/chrimp-observer-calibration/report.md

evidence:
  behavior_diagnosis: ../../behavior-synthesis-performance.md
  risk_model: notes/T001-risks.md
  tools_and_metrics: ../../performance.md
  native_challenges: /tmp/chr-native-challenge/report.md
  fairness_scaling: /tmp/chr-fairness-scaling-challenge/report.md
  fairness_scaling_scope: Relocation preserved outcomes but induced no material scaling regression.
  censored_comparison: /tmp/chr-censored-unified-comparison.json
  synthesis: /tmp/chrimp-all-synthesis-queries
  observer_checks: /tmp/chrimp-observer-calibration/endpoint-work-checks.json
  cpu_profile: /tmp/chrimp-profile-supervision-final/profile.json
  heap_profile: /tmp/chrimp-heap-final
