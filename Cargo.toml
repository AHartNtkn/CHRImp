[package]
name = "chr"
version = "0.1.0"
edition = "2024"

[features]
diagnostics = []

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
serde_json = { version = "1.0.151", features = ["unbounded_depth"] }

[profile.profiling]
inherits = "release"
debug = 2
strip = false
